const defaultContent = `this is the default content to be inserted into page. On and on and on. And on.`

export default function PageContent({ defaultContent }) {
  return <main className="bg-gray-100 rounded-md p-6"></main>
}
